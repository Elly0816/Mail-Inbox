export enum Endpoints {
  login = 'login',
  signup = 'signup',
}

export enum protectedEndpoints {
  logout = 'logout',
  message = 'message',
  user = 'user',
  thread = 'thread',
}

export enum Headers {
  access = 'access',
  refresh = 'refresh',
}
