export enum Endpoints {
  login = 'login',
  signup = 'signup',
}

export enum protectedEndpoints {
  logout = 'logout',
  message = 'message',
  user = 'user',
}

export enum Headers {
  access = 'access',
  refresh = 'refresh',
}
